{
  "name": "pwa-data-preloader",
  "version": "1.0.0",
  "description": "Progressive Web Application (PWA) data preloader to preload/pre-fetch data and caching or store in local storage before opening the opening the application on the screen through service workers and using google workbox.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shivam Oraon (shivamoraon)",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "idb": "^8.0.0",
    "workbox-cli": "^7.1.0"
  }
}
